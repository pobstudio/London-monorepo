import { Bound } from '../../../utils/src';
import { DIMENSIONS as SEASON_DIMENSIONS } from '@pob/common';

export const MIN_STRAND_PROPS_FROM_COMPOSITION = 125;
export const GRID_BOUNDS: Bound = [45, 72];
export const CIRCLE_RADIANS = Math.PI / 45;
export const DIMENSIONS: Bound = SEASON_DIMENSIONS['saga'];
